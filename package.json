{
  "name": "edvana-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && yarn install && yarn build",
    "install": "cd frontend && yarn install"
  },
  "engines": {
    "node": ">=18"
  }
}
